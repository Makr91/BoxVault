{
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": { "$ref": "#/$defs/identifier", "minLength": 1, "pattern": "\\S", "unique": "version" },
    "description": { "type": "string" }
  }
}
