{
  "type": "object",
  "required": ["organization"],
  "properties": {
    "organization": { "$ref": "#/$defs/slug", "unique": "global" },
    "org_code": { "$ref": "#/$defs/orgCode", "unique": "global" },
    "email": { "$ref": "#/$defs/email" },
    "description": { "type": "string" }
  }
}
