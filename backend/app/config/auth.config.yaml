jwt:
  jwt_secret:
    type: password
    value: boxvault-secret-key
    description: Secret key for JWT
  jwt_token_time_valid:
    type: integer
    value: 3600
    description: JWT token validity time in seconds
