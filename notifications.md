# Notifications from the STARTcloud UI estate

Nothing open. Every item lands here from another repository's
`notifications.md`, is done or moved to the repository that owns it, and
leaves. Asks of other repositories live in their own `NOTIFICATIONS.md`
(`../startcloud-ui/NOTIFICATIONS.md`,
`../authorization-server-private/NOTIFICATIONS.md`), never here.
